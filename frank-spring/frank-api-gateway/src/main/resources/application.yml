# application.yml
# Spring Boot application configuration
# -----------------------
# Minimal configuration for Frank-API-Gateway microservice 
#
# NOTES:
# - Reactive web application type is required for Spring Cloud Gateway
# - Basic routing is configured for hello, orchestrator, and SSE services
# - StripPrefix filter used as an example for path modification
# - Autoconfigure.exclude removed as keys are now correct
#
# Author: Edoardo Sabatini
# Date: 30 September 2025

# Server configuration
server:
  port: 8081

spring:
  application:
    name: frank-api-gateway
  main:
    web-application-type: reactive # Required for Spring Cloud Gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: hello-endpoint
              uri: http://localhost:8080
              predicates:
                - Path=/hello

            # The following route does not work because:
            # - the backend uses /frankorchestrator, not /orchestrator
            # - StripPrefix=1 breaks the dynamic path
            # - therefore it is commented out
            # - id: orchestrator-service
            #   uri: http://localhost:8080
            #   predicates:
            #     - Path=/orchestrator/**
            #   filters:
            #     - StripPrefix=1 # Example of a basic filter

            # The following route is not used by the new controller
            # - id: hello-sse-service
            #   uri: http://localhost:8080
            #   predicates:
            #     - Path=/sse/**
            #   filters:
            #     - StripPrefix=1 # Example of a basic filter

            # ✅ NEW ACTIVE ROUTE for the new endpoint
            - id: frank-orchestrator
              uri: http://localhost:8080
              predicates:
                - Path=/frankorchestrator/**
